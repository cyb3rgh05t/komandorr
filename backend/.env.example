# Server Configuration (for local development only)
# In Docker, these are set via docker-compose.yml environment variables
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================================
# NOTE: All other settings are now managed through config.json
# ============================================================================
# 
# The following settings should be configured via the Settings UI or by 
# editing backend/data/config.json directly:
#
# - Logging (level, file logging)
# - Timezone
# - CORS origins
# - Authentication (enabled, username, password)
# - API Tokens (GitHub, TMDB)
# - Plex Server (URL, token, server name)
#
# See: backend/data/config.json.example for the structure
#
